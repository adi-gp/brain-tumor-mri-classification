# Models

Trained Keras model files are generated here:

- `best_model.keras`
- `final_model.keras`
- `class_names.json`

Model binaries are ignored by Git because they can be large. For a portfolio repository, upload weights to GitHub Releases, Google Drive, or Hugging Face and link them from the README if needed.

